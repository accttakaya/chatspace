.side-bar
  .user-name
    .top-items
      .top-items__name
        = current_user.name
      .top-items__icons
        = link_to new_group_path(current_user), class: "top-items__icons__edit" do
          %i.fa.fa-edit
        = link_to edit_user_path(current_user),  class: "top-items__icons__setting" do
          %i.fa.fa-cog
  .group-list
    - current_user.groups.each do |group|
      .group-box
        = link_to group_messages_path(group), class: "group-box__link" do
          .group-box__link__groupName
            = group.name
          .group-box__link__newMessage
            = group.show_last_message